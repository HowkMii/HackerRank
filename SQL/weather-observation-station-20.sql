select cast (avg(1.0 *lat_n)as numeric(20,4))
from (select lat_n, ra=row_number()over(order by lat_n,id), rd=row_number()over(order by lat_n desc,id desc)from station)as x where ra between rd-1 and rd +1;